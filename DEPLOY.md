# 🚀 نشر موقع Skyline ومشاهدته على الجوال

الهدف: الحصول على **رابط على الإنترنت** تفتحه من متصفّح جوالك (Safari / Chrome) وتستخدم الموقع بالكامل.

أسهل طريقة مجانية هي منصة **Render**. الخطوات (تستغرق ~5 دقائق):

## الخطوات

1. **أنشئ حساباً** على [render.com](https://render.com) (مجاناً) — سجّل الدخول بحساب GitHub الخاص بك.

2. من لوحة Render اضغط **New +** ثم اختر **Blueprint**.

3. اختر مستودع GitHub: **`nj3095789-blip/-`** ثم الفرع **`claude/skyline-immigration-portal-5aignp`**.
   - سيكتشف Render ملف `render.yaml` تلقائياً ويقرأ كل الإعدادات.

4. اضغط **Apply** / **Create**. سيبدأ البناء تلقائياً (يثبّت الحزم ويشغّل الخادم).

5. انتظر حتى تتحول الحالة إلى **Live** 🟢، وسيظهر لك رابط مثل:
   ```
   https://skyline-immigration.onrender.com
   ```

6. **افتح هذا الرابط من جوالك** — الموقع يعمل بالكامل ويمكنك تصفّحه ومشاركته. ✅

## الدخول كمشرف
- البريد: `admin@skyline.com`
- كلمة المرور: `Skyline@Admin2026`
- ⚠️ غيّر كلمة المرور من إعدادات Render (Environment → `ADMIN_PASSWORD`) بعد أول دخول.

## تفعيل المدفوعات الحقيقية (اختياري)
الموقع يعمل بوضع المحاكاة افتراضياً. لتفعيل دفع حقيقي:
1. من Render: **Environment** → أضف قيمتي `STRIPE_SECRET_KEY` و `STRIPE_PUBLISHABLE_KEY` من [حسابك في Stripe](https://dashboard.stripe.com/apikeys).
2. احفظ — سيعيد Render النشر تلقائياً، وتصبح كل المدفوعات حقيقية.

## ملاحظات الخطة المجانية
- قاعدة البيانات (SQLite) على الخطة المجانية **مؤقتة** وقد تُصفَّر عند إعادة التشغيل — مناسبة للعرض والتجربة. للإنتاج الدائم رقِّ الخطة أو اربط قاعدة بيانات خارجية.
- الخدمة المجانية «تنام» بعد فترة خمول، وقد يتأخر أول فتح ~30 ثانية ثم تعمل بسرعة.

---

### بدائل أخرى
- **Railway** ([railway.app](https://railway.app)): New Project → Deploy from GitHub → اختر المستودع. اضبط أمر التشغيل `npm start`.
- **Fly.io / VPS**: أي خادم يدعم Node.js ≥ 18 يكفي — `npm install && npm start`.
